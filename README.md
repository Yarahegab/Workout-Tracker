# Workout-Tracker
A Java-based command-line workout tracker that lets users create dated workout sessions, add exercises with custom sets, reps, distance, or duration, view all logged workouts, and reset specific sessions. Built with OOP principles using inheritance and polymorphism.
